overfitting : train은 잘 맞추지만 test 를 예측하지 못함

pca : 변수간 중첩된 정보를 없애자

x1 과 x2의 선형결합인 새로운 축을 만들기 p15

** 범주형 변수는 pca 사용할 수 없음

z1과 z2 는 수직 => corr=0
세개인 경우는 분산이 큰 것 기준으로 z3 추가

p30 시그마 : x의 cov matrix

p33 세개 아니면 두개의 변수를 선택한다고 이해

p34
2. 정규화시키면 centered data matrix
eigen value(=분산) 가 큰 순서로 나열을 해주면 분산이 큰 순서로 행렬이 생성
